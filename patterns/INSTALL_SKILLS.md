When adding skills make sure to read the claude code documentation online.

Create a Claude skill for when there are changes to be made to the shared package, to tell claude how the shared package is structured and to provide exmaples for different implementations.

Create a Claude skill for when a backend change needs to be made to tell claude how the backend is structured and provide examples for different implementations.

Create a skill for when a frontend change needs to be made to tell claude how the frontend is structured and provide examples for different implementations.
